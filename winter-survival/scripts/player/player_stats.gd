extends Node
## DEPRECATED (M1): the survival stats moved to the server-side `StatsComponent`
## (scripts/player/stats_component.gd) with the owner mirror in `PlayerState`. This file only remains so the
## working tree needs no deletion; remove it.
