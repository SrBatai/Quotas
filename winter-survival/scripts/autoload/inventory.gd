extends Node
## DEPRECATED (M1): `Inventory` is no longer an autoload. Each player owns an `InventoryComponent`
## on the server (scripts/player/inventory_component.gd) mirrored to its owner through `PlayerState`
## (scripts/player/player_state.gd). This file only remains so the working tree needs no deletion; remove it.
