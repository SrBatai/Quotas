extends Node
## DEPRECATED (M1): `QuestManager` is no longer an autoload. Each player owns a `QuestComponent` on the
## server (scripts/player/quest_component.gd) whose state is mirrored to its owner through `PlayerState`.
## This file only remains so the working tree needs no deletion; remove it.
