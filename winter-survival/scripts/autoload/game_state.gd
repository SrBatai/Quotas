extends Node
## DEPRECATED (M1): `GameState` is no longer an autoload. The clock/weather live in the replicated
## `WorldState` node (scripts/net/shared/world_state.gd) and the scene flow in the `GameFlow` autoload
## (scripts/autoload/game_flow.gd). This file only remains so the working tree needs no deletion; remove it.
